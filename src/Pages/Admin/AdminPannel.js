import React from 'react';
import Error from '../../Components/Error/Error';

const AdminPannel = () => {
    return (
        <div>
            <Error></Error>
        </div>
    );
};

export default AdminPannel;